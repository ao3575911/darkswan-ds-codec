# darkswan-ds-codec
Line-addressable DarkSwan (.ds) encoder/decoder that turns bytes into nibble-sized whitespace runs.   Ships DS2 metadata checksum support, DS1 legacy mode, CLI + Python API, inspect/verify tooling,   samples and tests. Sensitive to any whitespace drift for tamper detection. Includes manual encoding   tips.
